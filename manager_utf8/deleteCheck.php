<?php
 $arr = array(
    ); 
 reset($arr); 

/*

 foreach ($arr as $value) { 

    $urlForAllatBo = $value;
    $key09 = 'b1456ed3-439d-47e0-b50d-839757620fd6';
    $count = 1;
	$ch = curl_init();                                 
	curl_setopt($ch, CURLOPT_URL, $urlForAllatBo);         
	curl_setopt($ch, CURLOPT_CONNECTTIMEOUT, 2000);    
	curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
    curl_setopt($ch, CURLOPT_TIMEOUT, 2000); 
	curl_setopt($ch, CURLOPT_HTTPHEADER, array('Content-Type:application/json', 'Authorization:'.$key09));
    curl_setopt($ch, CURLOPT_CUSTOMREQUEST, 'DELETE');
	$responseAllatBo = curl_exec($ch);
echo "1";

	curl_close($ch);
 } 
 */
?>